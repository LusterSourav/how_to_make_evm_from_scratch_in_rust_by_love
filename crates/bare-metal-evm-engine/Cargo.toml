[package]
name = "bare-metal-evm-engine"
version = "0.3.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "EVM execution engine: fetch-decode-execute loop, stack, memory, dispatch table, and core opcode handlers"
keywords = ["evm", "engine", "interpreter", "opcodes", "no-std"]
categories = ["no-std", "data-structures"]
publish = ["crates-io", "github"]

[dependencies]
bare-metal-evm-types = { workspace = true }
bare-metal-evm-gas = { workspace = true }
