[package]
name = "bare-metal-evm-gas"
version = "0.3.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "EVM gas metering: memory expansion, intrinsic gas, access sets, SSTORE, CALL, and GasMeter orchestrator"
keywords = ["evm", "gas", "metering", "sstore", "no-std"]
categories = ["no-std", "data-structures"]
publish = ["crates-io", "github"]

[dependencies]
bare-metal-evm-types = { workspace = true }

[dev-dependencies]
proptest = "1.5"
